SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Inngest development key
INNGEST_DEV=1
# Inngest production key
# INNGEST_EVENT_KEY=
# INNGEST_SIGNING_KEY=

# Optional: where contact messages are delivered.
# Defaults to the owner's email if unset.
CONTACT_TO_EMAIL=

# Canonical site URL — used by the sitemap, robots.txt and SEO metadata.
NEXT_PUBLIC_SITE_URL=
