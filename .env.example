# Resend — required by the contact form's API route (/api/contact).
# Create a key at https://resend.com/api-keys, then copy this file to
# `.env.local` and fill it in.
RESEND_API_KEY=

# Optional: where contact messages are delivered.
# Defaults to the owner's email if unset.
CONTACT_TO_EMAIL=
