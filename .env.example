SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Inngest development key
INNGEST_DEV=1
# Inngest production key
# INNGEST_EVENT_KEY=
# INNGEST_SIGNING_KEY=

# Required — where contact messages are delivered.
CONTACT_TO_EMAIL=

# Required — canonical site URL, used by the sitemap, robots.txt and SEO metadata.
NEXT_PUBLIC_SITE_URL=
