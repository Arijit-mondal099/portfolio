# Approve post-install build scripts for native deps.
# pnpm v10+ blocks build scripts by default for security; we opt these in
# explicitly so installs don't fail: `sharp` powers next/image optimization,
# `unrs-resolver` backs the ESLint TypeScript import resolver.
allowBuilds:
  sharp: true
  unrs-resolver: true
