/**
 * Canonical site URL, used by the sitemap, robots rules, and SEO metadata.
 * Set `NEXT_PUBLIC_SITE_URL` in production (the fallback is a placeholder).
 */
export const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://arijit-mondal.vercel.app";
