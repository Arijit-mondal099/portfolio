/**
 * Home page — composes the portfolio sections.
 *
 * Sections are added phase by phase (Hero, About, Contributions, Skills,
 * Projects, Contact, Footer). For now this is a placeholder that confirms the
 * theme, fonts, and layout shell render correctly.
 */
export default function Home() {
  return (
    <main className="flex flex-1 items-center justify-center px-6">
      <p className="text-sm text-muted-foreground">
        Portfolio — coming together, section by section.
      </p>
    </main>
  );
}
