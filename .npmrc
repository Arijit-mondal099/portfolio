# Fail the install fast when the runtime doesn't satisfy package.json "engines"
# (Node version is also pinned in .nvmrc).
engine-strict=true

# Automatically install peerDependencies. This is pnpm's default; we declare it
# explicitly so the behavior is obvious and stable across pnpm versions.
auto-install-peers=true
